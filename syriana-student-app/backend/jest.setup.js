// Jest setup file (kept empty intentionally)
// in backend/package.json. Add global test configuration here if needed later.

// Example: extend Jest matchers or set up global mocks.
// jest.setTimeout(10000);

module.exports = {};
